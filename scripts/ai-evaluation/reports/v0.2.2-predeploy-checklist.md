# v0.2.2 部署前检查报告

**日期:** 2026-08-15  
**版本:** `v0.2.2`  
**范围:** Beta2.2 AI 识别正式上线 + 设置页版本信息展示  
**约束遵守:** 未改 AI_PROMPT / evaluation / 检查更新逻辑 / 数据同步逻辑

---

## 1. Beta2.2 当前状态（确认）

| 能力 | 状态 | 位置 |
|------|------|------|
| 默认 `visionPipeline: 'beta2'` | ✅ | `app.ts` DEFAULT_STORE |
| 生产 `AI_PROMPT` 原文保留 | ✅ | `app.ts`（不全文替换） |
| product-focus ×3 + 字段独立 ×3 | ✅ | `aiBeta22.ts` → `getVisionPrompt` |
| OCR purchaseDate 后台 merge | ✅ | `scheduleOcrPurchaseDateMerge`（不 `Promise.all`） |
| OCR timeout 12s | ✅ | `ocrClient.ts` + 调用处 |
| Vision timeout 20s AbortController | ✅ | `external.ts` `callVisionAPI` |
| Timeout → 结束 loading + 可理解提示 | ✅ | `app.ts` Vision `.catch` |
| Beta1 回滚开关 | ✅ | 设置页「衣橱识别版本」 |
| 保存 / UI 表单流程 | ✅ | 原路径；OCR 仅写 buyDate |

**结论:** Beta2.2 识别链路已具备上线条件（默认开启、可回滚、双超时）。

---

## 2. 设置页版本展示

| 项 | 内容 |
|----|------|
| 新增 | 设置页「当前版本」卡片（只读） |
| 展示 | 应用版本 `v0.2.2`；衣橱识别 `Beta2.2` |
| 常量 | `src/wardrobe/version.ts` |
| 未改 | 「检查应用更新」「同步云端数据」按钮与逻辑 |

---

## 3. 版本号

| 文件 | 版本 |
|------|------|
| `package.json` | `0.2.2` |
| `package-lock.json`（root） | `0.2.2` |
| `src/wardrobe/version.ts` | `APP_VERSION = '0.2.2'` |

---

## 4. Build 验证

| 检查 | 结果 |
|------|------|
| `npm run typecheck` | ✅ 通过 |
| `npm run build` | ✅ 通过 |
| PWA SW stamp | ✅ `build=20260815072903` |
| 产物 | `dist/` 已生成 |

---

## 5. 本 release 改动文件（相对上线准备）

- `src/wardrobe/version.ts`（新增）
- `src/wardrobe/app.ts`（设置页版本卡片；既有 Beta2.2 能力）
- `package.json` / `package-lock.json`（版本号）
- （既有、本 checklist 确认）`aiBeta22.ts` / `ocrClient.ts` / `external.ts`

**未改:** prompts 全文、`scripts/ai-evaluation/**`、`pwa.ts` 更新检查、dataStore / 同步路径。

---

## 6. 部署前 Checklist

- [x] Beta2.2 默认开启、Beta1 可回滚  
- [x] Vision ≤20s / OCR ≤12s  
- [x] OCR 不阻塞表单  
- [x] 设置页显示 `v0.2.2` + Beta2.2  
- [x] typecheck + production build 通过  
- [ ] **待人工:** 部署到 Vercel / Cloudflare Pages（本报告不执行部署）  
- [ ] **待人工:** 生产环境上传 1–2 张订单截图：category / 日期 / 保存  
- [ ] **待人工:** 设置页确认版本文案与识别开关  
- [ ] **待人工:** PWA「检查应用更新」拉取新 SW（逻辑未改，部署后点一次即可）

---

## 7. 发布说明（建议）

**v0.2.2 — Beta2.2 衣橱识别**

- 默认启用 Beta2.2：分类主体优先 + 字段独立 + OCR 购买日期后台合并  
- Vision 20s / OCR 12s 超时，避免无限 loading  
- 设置页可回滚 Beta1；展示当前应用版本  

**已知限制（非阻塞）:** season 仍弱；Vision 字段有随机波动；浏览器 OCR 首次可能下载语言包（后台，不挡表单）。
